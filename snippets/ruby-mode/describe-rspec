# -*- mode: snippet; require-final-newline: nil -*-
# name: describe-rspec
# key: desc
# --
describe ${1:`(let ((fn (capitalize (file-name-nondirectory
                                 (file-name-sans-extension
				 (or (buffer-file-name)
				     (buffer-name (current-buffer))))))))
             (replace-regexp-in-string "_" "" fn t t))`} do
$0
end